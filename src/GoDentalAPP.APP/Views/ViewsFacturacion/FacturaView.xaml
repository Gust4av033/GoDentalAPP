<UserControl x:Class="GoDentalAPP.src.GoDentalAPP.APP.Views.ViewsFacturacion.FacturaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoDentalAPP.src.GoDentalAPP.APP.Views.ViewsFacturacion"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <ComboBox ItemsSource="{Binding TiposFactura}" SelectedItem="{Binding TipoFactura}"/>
            <TextBlock Text="Cliente:" Margin="5,0"/>
            <ComboBox ItemsSource="{Binding Clientes}" DisplayMemberPath="NombreCompleto"
                      SelectedValue="{Binding ClienteId}" SelectedValuePath="ClienteId" Width="200"/>
            <Button Content="Buscar Producto" Command="{Binding BuscarProductoCommand}"/>
        </StackPanel>

        <!-- Detalles -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Detalles}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Producto.CodigoBarras}"/>
                <DataGridTextColumn Header="Producto" Binding="{Binding Producto.NombreInsumo}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnitario, StringFormat=C2}"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}"/>
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C2}"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Eliminar" Command="{Binding DataContext.EliminarDetalleCommand, 
                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totales y acciones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Subtotal:" Margin="5"/>
            <TextBox Text="{Binding Subtotal, StringFormat=C2}" Width="100" IsReadOnly="True"/>
            <TextBlock Text="IVA:" Margin="5"/>
            <TextBox Text="{Binding Iva, StringFormat=C2}" Width="100" IsReadOnly="True"/>
            <TextBlock Text="Total:" Margin="5" FontWeight="Bold"/>
            <TextBox Text="{Binding Total, StringFormat=C2}" Width="120" IsReadOnly="True" FontWeight="Bold"/>
            <Button Content="Generar Factura" Command="{Binding GenerarFacturaCommand}" Margin="10" Padding="10 5"/>
            <Button Content="Generar Crédito Fiscal" Command="{Binding GenerarCreditoFiscalCommand}" Margin="10" Padding="10 5"/>
        </StackPanel>
    </Grid>
</UserControl>
